# 第二次改动的部分，更改后的文件也在files_changed文件夹中

1.

### iree\compiler\src\iree\compiler\Dialect\NPUFuseOp

文件夹下的文件内容有更改，可以直接用files_changed/NPUFuseOp文件夹替换原本的文件夹

```
BufferizableOpInterfaceImpl.cpp
CMakeLists.txt
NPUFuseDialect.cpp
NPUFuseDialect.h
NPUFuseOps.cpp
NPUFuseOps.h
NPUFuseOps.td
```



## 2.

### iree\compiler\src\iree\compiler\Codegen\LLVMCPU

替换这些文件：(新文件在files_changed/LLVMCPU文件夹中)

```
FuseConvVecOp.cpp
FuseConvVecOp.h
FuseConvVecFunc.cpp
FuseConvVecFunc.h
```

### iree\compiler\src\iree\compiler\Codegen\LLVMCPU\Passes.cpp

```cpp
// 开头部分新加的一个预选项：
// use npu op fast math flags for the fused conv patterns
static llvm::cl::opt<bool> clEnableNpuFuseOpFastMath(
  "iree-llvmcpu-npufuseop-fast-math",
  llvm::cl::desc("Enable fast-math for npufuseop fused conv chains"),
  llvm::cl::init(false));


//插入的FuseConvVecOpPass处
void addConvTileAndDecomposeExpertPassPipeline(
    OpPassManager &passManager, TilingConfig &tilingConfig,
    LLVMCPUPipelineOptions &pipelineOpt) {
  addTileAndDistributePasses(passManager);

  OpPassManager &nestedModulePM = passManager.nest<ModuleOp>();
  // Run LLVMTileAndFuse firstly in case that we have fill + conv + generic
  // ops. At this stage, we do not apply vectorization. The reduction dim won't
  // get tiled if the case is conv + generic op. In this case, we have to tile
  // along reduction dim again, which needs them to be Linalg ops form.

  nestedModulePM.addNestedPass<func::FuncOp>(createLLVMCPUTileAndFusePass(
      tilingConfig.getVectorCommonParallelLevel()));

  // Here adds a new pass to replace fuse op patterns
  nestedModulePM.addNestedPass<func::FuncOp>(createFuseConvVecOpPass(clEnableNpuFuseFastMath));
```

改动了iree\compiler\src\iree\compiler\Codegen\Common\GenericVectorization.cpp

(Files_changed/GenericVectorization)

这里根据融合的算子加了命名后缀，避免多层网络编译的时候函数声明冲突



改动了iree\compiler\src\iree\compiler\Codegen\LLVMCPU\ConvertNPUOps.cpp

(Files_changed/LLVMCPU/ConvertNPUOps.cpp)

删掉了之前保留的注释块，优化了一下声明的过程，避免重复
